name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: See env
      run:
        - env
        - git add .
        - git commit -m "autoload"
        - git push
 
